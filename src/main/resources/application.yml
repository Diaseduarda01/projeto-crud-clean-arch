spring:
  application:
    name: vampiros-project-api

  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org.springframework: DEBUG
    gerenciamento.vampiro: DEBUG